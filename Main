repeat 
    wait() 
until game:IsLoaded() and game.Players.LocalPlayer and 
      game.Workspace:FindFirstChild("__Extra") and 
      game.Workspace:FindFirstChild("__Main")

while true do
    wait(300) -- Chờ 5 phút (300 giây)
    game:GetService("VirtualUser"):CaptureController()
    game:GetService("VirtualUser"):ClickButton2(Vector2.new())
end

local OrionLib = loadstring(game:HttpGet("https://raw.githubusercontent.com/jensonhirst/Orion/main/source"))()

local Window = OrionLib:MakeWindow({
    Name = "Pịa Hub - Pịa xào me",  -- Thêm phần text "Pịa xào me" phía trước
    HidePremium = false,
    SaveConfig = false,
    ConfigFolder = "AutoHub"
})

local settingsFile = "autohub_settings.json"
local HttpService = game:GetService("HttpService")

-- Đọc settings
local settings = {}
pcall(function()
    if isfile(settingsFile) then
        settings = HttpService:JSONDecode(readfile(settingsFile))
    end
end)

-- Lưu settings
local function saveSettings()
    writefile(settingsFile, HttpService:JSONEncode(settings))
end

local InfoTab = Window:MakeTab({
    Name = "Info",
    Icon = "rbxassetid://83969479610474",
    PremiumOnly = false
})

-- Thông báo cập nhật
InfoTab:AddParagraph("🛠️ Cập nhật mới nhất", [[
📌 - Version: v1.1
🆕 + Thêm Auto Join Castle
🔧 + Cải thiện hiệu xuất tính năng
💡 + Lưu setting khi telepoint
⚔️ + Arise đang có "Jinwoo"-"Pucci"-"Denji"
⛩️ + Check double "bú sjw N 🤑"
🌡 + Fix ping cao 🗿
+++ Add auto click
]])

local MainTab = Window:MakeTab({
    Name = "Main",
    Icon = "rbxassetid://124598949660449",
    PremiumOnly = false
})

local toggleList = {
    {
        Name = "Auto Bypass",
        Key = "AutoBypass",
        Script = "https://raw.githubusercontent.com/skyemngu13/hee/refs/heads/main/bypass"
    },
    {
        Name = "Auto Kill Dungeon",
        Key = "AutoKillDungeon",
        Script = "https://raw.githubusercontent.com/skyemngu13/hee/refs/heads/main/automobdungeon"
    },
    {
        Name = "Auto Farm Mori",
        Key = "AutoFarmMori",
        Script = "https://raw.githubusercontent.com/skyemngu13/hee/refs/heads/main/mori"
    },
    {
        Name = "Auto Join Castle",  -- 👈 Toggle mới
        Key = "AutoJoinCastle",
        Script = "https://raw.githubusercontent.com/skyemngu13/hee/refs/heads/main/castle"
    }
}

-- Tạo toggle
for _, t in ipairs(toggleList) do
    settings[t.Key] = settings[t.Key] or false

    MainTab:AddToggle({
        Name = t.Name,
        Default = settings[t.Key],
        Callback = function(val)
            settings[t.Key] = val
            saveSettings()

            if val then
                -- Chạy sau 1 frame, tránh crash UI
                task.spawn(function()
                    pcall(function()
                        loadstring(game:HttpGet(t.Script))()
                    end)
                end)
            end
        end
    })
end

-- Tạo Tab Misc với Toggle AutoClick (Tab này sẽ là tab thứ 3)
local MiscTab = Window:MakeTab({
    Name = "Misc",
    Icon = "rbxassetid://95758233681936",
    PremiumOnly = false
})

MiscTab:AddToggle({
    Name = "AutoClick",  -- Toggle mới
    Default = settings["AutoClick"] or false,
    Callback = function(val)
        settings["AutoClick"] = val
        saveSettings()

        if val then
            -- Chạy script AutoClick sau 1 frame
            task.spawn(function()
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/skyemngu13/hee/refs/heads/main/autoclick"))()
                end)
            end)
        end
    end
})

-- Sau khi UI xong → chạy các script đã bật
task.spawn(function()
    task.wait(1) -- đảm bảo UI vẽ xong
    for _, t in ipairs(toggleList) do
        if settings[t.Key] then
            pcall(function()
                loadstring(game:HttpGet(t.Script))()
            end)
        end
    end
end)
